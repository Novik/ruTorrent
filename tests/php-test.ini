; The suite runs against these settings and no others: -c replaces the
; system php.ini, so a developer's local configuration cannot change what
; a test means.
zend.assertions = 1
error_reporting = -1
display_errors = 1
