#summary Главная страница.
#labels Featured

== Введение ==

ruTorrent был разработан на базе [http://www.utorrent.com/documentation/webui WebUI] к [http://utorrent.com чTorrent], соответственно, его внешний вид очень напоминает "родителя". Собственно, своим названием ruTorrent тоже обязан этому самому родителю - чTorrent + rTorrent = ruTorrent.

== Чего требует для работы ==

 * rTorrent версии не менее 0.8.2, скомпилированный с поддержкой XML-RPC. Если Вы не знаете, где взять таковой для Вашей системы, рекомендую посетить [http://libtorrent.rakshasa.no страницу автора rTorrent].
 * Веб-сервер с поддержкой php 5.0.
 * Браузер, как локальный клиент.
   * IE 7.0, Firefox 2.x, Firefox 3.x - оптимальный выбор.
   * IE 8.0 - в стандартном режиме пока поддерживается ограниченно. В режиме совместимости с IE7 работает нормально.
   * IE 6.0 - более не поддерживается.
   * Konqueror 3.5.9 - работает (за исключением диаграммы скорости и плагина trafic).
   * Opera 9.x - работает.
   * Safari 3.x - работает. Возможны проблемы с не отображением картинок при использовании протокола https. Полагаю, ошибка в браузере. Нужно подстраивать сервер, чтобы не держал keep-alive соединения, если клиент пользует Safari.
   * Google Chrome 0.2.149.30 - работает. Возможны проблемы, аналогичные Safari.

 Прочие браузеры не проверял.

== Установка и настройка ==

 # Скачать архив ruTorrent и развернуть его содержимое в корень веб-сервера. [GetFromSVN Либо забрать последнюю версию ruTorrent с svn]. Желательно проделать эту операцию из под пользователя, под которым работает веб-сервер, чтобы потом не разбираться с правами файлов/директорий.
 # Добавить в конфигурационный файл rTorrent строчку `scgi_port = 127.0.0.1:5000` (если ее там еще нет).
 # [WebserverSetup Настроить scgi-gateway в веб-сервере] для проброса запросов с /RPC2 на порт, который слушает rTorrent. Либо просто поставить [Plugins плагин] [PluginRPC RPC].
 # Набрать в браузере `http://адрес_сервера:порт_сервера/rtorrent`

== Чего не может ==

 * Разделение доступа. ruTorrent (как и rTorrent впрочем) представляет собой монопользовательскую систему. Если Вам нужно разделение прав на работу с торрентами между несколькими пользователями - используйте несколько копий того и другого.

== Некоторые неочевидные моменты ==

 * Галку "Запрашивать сообщения трекера" в настройках ruTorrent можно включать только в том случае, если Вы используете версию rTorrent >= 0.8.5. В противном случае ее включение может привести к потере работоспособности ввиду некорректной обработки rTorrent сообщений трекера, содержащих не UTF-8 символы.
 * Настройки rTorrent, изменяемые пользователем через соответствующий диалог ruTorrent валидны только на время текущей сессии rTorrent. После рестарта последнего они будут утеряны.
 * Если вместо чисел больших 2 гигабайт ruTorrent показывает отрицательные числа, следует рекомпилировать rTorrent с более свежей версией xmlrpc-c. Минимально необходимая версия данной библиотеки - 1.11.
 * При изменении номера порта, на котором rTorrent будет ожидать запросы, необходимо так же внести коррективы в конфигурацию ruTorrent - указать этот же порт в файле rtorrent/conf.php в переменной `$scgi_port`.
 * Авторизация доступа реализуется полностью средствами используемого веб-сервера и от ruTorrent не зависит. Защищать нужно URL /rtorrent и, если используется scgi модуль веб-сервера - /RPC2. В случае использования вместо scgi модуля веб-сервера [Plugins плагина] ruTorrent [PluginRPC RPC] URL /RPC2 защищать не нужно.
 * В случае необходимости иметь несколько копий rTorrent/ruTorrent на одной машине, Вам может понадобиться изменить имя умолчательной точки монтирования XMLRPC (/RPC2). Сделать это можно внеся изменения в файл rs.js. В случае использования вместо scgi модуля веб-сервера [Plugins плагина] ruTorrent [PluginRPC RPC] об этом заботиться не нужно.

== Особенности браузеров ==

 * Для работы контекстного меню и отображения информации в статус-баре в Firefox и Opera необходимо в настройках браузера разрешить javascript-у выполнять соотв. операции.
 * Имеющиеся клавиатурные хоткеи можно посмотреть нажав F1. Более-менее корректно работает это пока только в Firefox.
 * Ряд аддонов к браузерам может привести к полной или частичной неработоспособности ruTorrent. Например, установка плагина Firebug к Firefox ведет к сильным утечкам памяти. Установка плагина All-in-One Gestures к Firefox ведет к неработоспособности контекстного меню. И т.п.

== Внешние ссылки  ==

 * [http://donik.org/wiki/index.php/RuTorrent_%D1%83%D1%81%D1%82%D0%B0%D0%BD%D0%BE%D0%B2%D0%BA%D0%B0_%D0%B8_%D0%BD%D0%B0%D1%81%D1%82%D1%80%D0%BE%D0%B9%D0%BA%D0%B0_%D0%BF%D0%BE%D0%B4_Gentoo RuTorrent установка и настройка под Gentoo]

== [ErrorMessages Сообщения об ошибках] ==

== [Plugins Плагины] ==

== [Versions История версий] ==

== [http://forums.rutorrent.org Форум] ==