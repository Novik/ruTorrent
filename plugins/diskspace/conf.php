<?php

$diskUpdateInterval = 10;	// in seconds
$notifySpaceLimit = 512;	// in Mb
$partitionDirectory = rTorrentSettings::get()->directory; // mount point of a torrents partition, download directory by default
